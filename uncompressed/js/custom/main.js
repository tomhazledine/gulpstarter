console.log('scripts are still working');

// Check that ES6 gets properly transpiled.
// (`let` should be turned into `var` etc.)
let something = 'something';

let test_function = input => input * 2;

